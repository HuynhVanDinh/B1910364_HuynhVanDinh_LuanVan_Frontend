import { FilerByMuc2Pipe } from './filer-by-muc-2.pipe';

describe('FilerByMuc2Pipe', () => {
  it('create an instance', () => {
    const pipe = new FilerByMuc2Pipe();
    expect(pipe).toBeTruthy();
  });
});
