import { FilterByMucPipe } from './filter-by-muc.pipe';

describe('FilterByMucPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByMucPipe();
    expect(pipe).toBeTruthy();
  });
});
